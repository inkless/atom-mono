name: Build and E2E Tests

on:
  push:
    branches: ['*']

jobs:
  # build:
  #   type: build
  #   params:
  #     platform: ios
  #     profile: ios-simulator-preview
  end-to-end-test:
    # needs: [build]
    type: maestro
    params:
      build_id: '4bccd8d6-54eb-4e8a-b8e8-bd634f745ab1' #${{ needs.build.outputs.build_id }}
      flow_path: ['./maestro/flow.yaml']

