name: Run Unit Tests

on:
  push:
    branches: ['*']

build:
  name: Build Android app
  steps:
      - name: upgrade pnpm
        run: npm i -g pnpm
      - uses: eas/checkout
      - uses: eas/install_node_modules
      - uses: eas/prebuild
      - uses: eas/run_gradle
          inputs:
            command: :app:lint